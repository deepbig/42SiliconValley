# 20) list all existing users on the VM.

# /etc/passwd file contains one line for each user account, with seven fields delimited by colons This is a text file.

cat /etc/passwd | awk -F : '{print $1}'
